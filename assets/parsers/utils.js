import {cleanText,lines,findAnyDate,findDate,findDateRange} from "../core/utils.js?v=98.0.0";
import {looksNoise,explicitStatus,sortRecent,normalizeEvent} from "../core/events.js?v=98.0.0";

export const PARSER_UTILS=Object.freeze({
  cleanText,lines,looksNoise,explicitStatus,findAnyDate,findDate,findDateRange,sortRecent,normalizeEvent
});
